	file := "./" + "log" + ".txt"
	logFile, err := os.OpenFile(file, os.O_RDWR|os.O_CREATE|os.O_APPEND, 0766)
	if err != nil {
		panic(err)
	}
	filelog.SetOutput(logFile) // 将文件设置为log输出的文件
	filelog.SetPrefix("[logTool]")
	filelog.SetFlags(filelog.LstdFlags | filelog.Lshortfile | filelog.LUTC)
